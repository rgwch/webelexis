version: '2'

services:
  etcd:
    image: docker.io/bitnami/etcd:3.5
    environment:
      - ALLOW_NONE_AUTHENTICATION=yes
    volumes:
      - etcd_data:/bitnami/etcd
    ports:
      - 2380:2380
      - 2379:2379
      - 4001:4001
volumes:
  etcd_data:
    driver: local
