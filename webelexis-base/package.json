{
  "name": "base-module",
  "version": "0.1.0",
  "description": "base module for webelexis",
  "author": "weirich@elexis.ch",
  "private": true,
  "license": "EPL",
  "scripts": {
    "clean": "rimraf dist",
    "build:client": "npm run create:dirs && (npm run compile:jade && r.js -o build-debug.js)",
    "create:dirs": "mkdirp dist/web",
    "compile:jade": "jade src/main/resources/web -o dist/web",
    "compile:java": "javac -g:none -encoding utf-8 -source 7 -target 7 -cp \"${VERTX_HOME}/lib/*\" src/main/java/ch/rgw/vertx/*.java src/main/java/ch/webelexis/agenda/*.java -d dist",
    "preverticle": "npm run compile:java && cp src/main/resources/webelexis-core.js dist && cp cfglocal.json dist",
    "verticle": "cd dist && vertx run webelexis-core.js -conf cfglocal.json",
    "premodule": "npm run clean && npm run build:client && npm run preverticle && cp src/main/resources/mod.json dist",  
    "module": "ncp dist ${VERTX_MODS}/dist && mv ${VERTX_MODS}/dist ${VERTX_MODS}/ch.webelexis~${npm_package_name}~${npm_package_version}",
    "start": "npm run build:client && npm run verticle",
    "env": "env"
  },
  "devDependencies": {
    "jade": ">=1.9.0",
    "requirejs": ">=2.1.16",
    "bootstrap": ">=3.3.2",
    "rimraf": "latest",
    "mkdirp": "latest",
    "ncp": "latest"
  }
}
