package ch.rgw.elexis.konsdecorator.base

class DecoratedKons(decorations: List[Decoration]) {
	def getSegments : List[Segment] = List()
	
	def getValues : List[Value] = List()
		  
	def parse(text: String) = {
	  
	}
	
}